const numero1 = 20;
const numero2 = "20";
// parseInt() es una función que convierte un string a un número entero
console.log(parseInt(numero2)); // Convertir un string a un número entero

// Método toString() convierte un número a un string
console.log(numero1.toString()); // Convertir un número a un string